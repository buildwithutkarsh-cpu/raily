// AppLayout (in app/app/layout.tsx) manages all content rendering
// based on the active sidebar section. This page is the entry point
// for the /app route — no additional content is needed here.
export default function AppPage() {
  return null;
}
